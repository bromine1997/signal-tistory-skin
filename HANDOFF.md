# Signal 프로젝트 인수인계

이 문서는 기존 대화 기록이 없는 사람이나 AI 에이전트가 다른 컴퓨터에서 Signal 티스토리 스킨 작업을 바로 이어갈 수 있도록 작성한 기준 문서다.

- 최종 갱신일: 2026-08-18
- 현재 스킨 버전: `0.5.2`
- 공개 저장소: <https://github.com/bromine1997/signal-tistory-skin>
- 기본 브랜치: `main`
- 최신 커밋은 `git log -1 --oneline`으로 확인

실제 블로그 주소는 공개 배포 문서에 고정하지 않는다. 라이브 검수가 필요하면 사용자에게 현재 주소를 확인한다.

## 1. 시작 배경과 목표

사용자는 기존 GitHub Pages 블로그를 티스토리로 옮기기로 했다. 콘텐츠와 이미지는 사용자가 직접 이전하며, 이 저장소는 **콘텐츠가 아니라 티스토리 스킨 코드**만 관리한다.

확인된 목표는 다음과 같다.

1. 기존 GitHub 블로그의 회로도·임베디드 분위기를 티스토리에서도 유지한다.
2. 티스토리의 카테고리, 검색, 댓글, 관련 글, 방문자 통계 등 운영 기능을 사용한다.
3. 이미지 확대, 목차, 코드 복사 등 기술 글을 읽기 편한 기능을 추가한다.
4. 누구나 배포할 수 있도록 개인 URL이나 기술 스택을 스킨에 고정하지 않는다.
5. 기능을 과도하게 늘리지 않고 글 읽기에 필요한 요소만 남긴다.

블로그 이전에 대한 개인적인 추가 사유는 대화에서 확인되지 않았다. 확인되지 않은 이유를 임의로 만들어 문서나 소개 글에 쓰지 않는다.

## 2. 작업 범위와 합의 사항

이 저장소가 담당하는 범위:

- 티스토리 스킨 HTML, CSS, JavaScript
- 티스토리 치환자 연결
- 반응형 레이아웃과 접근성
- 배포 ZIP 생성
- 설치 및 유지보수 문서

사용자가 직접 담당하는 범위:

- 기존 글과 이미지의 티스토리 이전
- 티스토리 관리자 화면에서 스킨 등록 및 적용
- 프로필, 블로그 설명, 카테고리 등 운영 데이터 입력

사용자 요청에 따라 기본 구성에서 제외한 기능:

- 인기 글과 공지 위젯
- 무한 스크롤
- 고정 GitHub·포트폴리오 링크
- 특정 사용자의 STM32·ESP32·AVR 같은 기술 스택 문구
- 방문자에게 노출되는 관리자 메뉴

새 요청 없이 위 기능을 기본값으로 되살리지 않는다.

## 3. 현재 화면 구조

### 상단바

- 블로그 로고
- 카테고리 드롭다운
- 데스크톱에서는 검색 아이콘 아래, 모바일에서는 헤더 아래에 펼쳐지는 검색창
- 라이트·다크 모드 버튼
- 모바일 메뉴 버튼

검색은 `0.3.0`에서 사이드바에 있었지만 `0.4.0`부터 다시 상단바로 이동했다.

### 왼쪽 사이드바

- 프로필 이미지: `[##_image_##]`
- 작성자명: `[##_blogger_##]`
- 블로그 설명: `[##_desc_##]`
- 선택형 추가 소개 문구: `[##_var_profile-intro_##]`
- 오늘·어제·전체 방문자 수
- 카테고리
- 소유자에게만 보이는 글쓰기·관리자 메뉴

상위 카테고리의 글 수는 티스토리가 직접 배정된 글만 제공하므로, `setupCategoryCounts()`가 하위 카테고리의 글 수를 합산해 사이드바와 상단 드롭다운에 표시한다.

위 프로필 치환자는 설치자의 티스토리 설정값이 런타임에 들어가는 것이므로 저장소에 저장된 개인정보가 아니다.

프로필 이름·이미지·기본 설명은 티스토리 **관리 → 블로그**에서 입력한다. 추가 문구는 스킨 적용 후 **꾸미기 → 스킨 편집 → 스킨 옵션 → 추가 소개 문구**에서 입력하며, 비워두면 해당 문단은 렌더링되지 않는다.

카테고리 이름과 계층은 티스토리 **콘텐츠 → 카테고리 관리**에서 편집한다. Signal은 티스토리가 출력하는 상위·하위 구조를 받아 상위 항목은 그룹 제목처럼, 하위 항목은 들여쓰기와 연결선으로 표시한다.

### 가운데 본문

- 목록 카드 전체 클릭과 별도 카테고리 링크
- 목록 댓글 수
- 글 링크와 제목별 섹션 링크 복사
- 글 상단에 쿼리와 해시를 제거한 전체 URL 표시
- 코드 언어 표시, 복사, 긴 코드 접기
- 티스토리 편집기가 만든 `&nbsp;` 빈 문단의 간격 정규화
- 표 가로 스크롤
- 이미지 라이트박스, 이전·다음 탐색, 이미지 개수
- 같은 카테고리의 다른 글
- 댓글
- 본문과 키워드 아래의 소유자 전용 수정·상태 변경·삭제 도구

### 오른쪽 사이드바

- 본문의 `h2`, `h3`에서 자동 생성한 목차
- 현재 읽는 항목 강조
- `0.4.0`에서 글자 크기, 줄 간격과 클릭 영역 확대
- 좁은 화면에서는 본문형 목차 또는 모바일 서랍으로 전환

### 반응형 폭 정책

- `820px` 이하: 단일 열 모바일 레이아웃
- `1320px` 이하: 우측 목차를 숨기고 본문형 목차 사용
- 일반 데스크톱: 가운데 본문 최대 `960px`
- `1680px` 이상: 전체 레이아웃 최대 `1720px`, 가운데 카드 최대 `1100px`
- 대형 화면의 문단·제목·목록·인용문: 최대 `54rem`
- 이미지·코드·표·영상: 넓은 카드 영역 활용

디자인 원칙은 **카드는 넓게, 글자는 읽기 좋은 폭으로, 미디어와 코드는 넓게**다.

## 4. 버전별 작업 내역

### 0.2.2

- 목록·카테고리의 글 중복 출력 제거
- 외부 프로필 링크와 고정 기술 스택 제거
- 글이 아닌 화면의 빈 목차 제거
- 댓글 치환자와 티스토리 코드 블록 호환성 개선
- 기본 관련 글과 커스텀 관련 글 중복 방지
- 전자부품 배경 장식 밀도와 반응형 표시 개선

### 0.3.0

- 글 목록 카드 전체 클릭과 댓글 수 표시
- 모바일 목차 서랍
- 글 및 섹션 링크 복사
- 코드 언어 표시와 긴 코드 접기
- 이미지 확대 이전·다음 탐색과 개수 표시
- 소유자 전용 글쓰기·관리자·글 관리 기능

### 0.4.0

- 검색을 상단 확장형으로 이동
- 동적 프로필 카드와 추가 소개 문구 옵션
- 전체 레이아웃과 본문 폭 확대
- 대형 모니터 전용 레이아웃
- 넓은 화면에서도 일반 글의 읽기 폭 제한
- 오른쪽 목차 가독성과 클릭 영역 개선
- 긴 관련 글 제목의 두 줄 제한과 넘침 방지

### 0.5.0

- 검색창을 아이콘과 연결된 데스크톱 팝오버로 변경
- 모바일 검색창을 헤더 아래 전체 너비로 표시
- 홈의 중복 `전체 글` 목록 헤더 제거
- 글 상단에 정리된 URL과 복사 기능 표시
- 글 관리 기능을 본문과 키워드 아래로 이동
- 날짜·카테고리·키워드·본문·목록·관련 글 타이포그래피 확대
- 목록 썸네일의 안쪽 여백과 테두리 추가
- 상위·하위 카테고리의 시각적 위계 개선

### 0.5.1

- 이전·다음 글 탐색을 제거하고 같은 카테고리 관련 글만 유지
- 관련 글 제목을 `이 카테고리에서 더 읽기`로 변경
- 방문자 통계를 카테고리 아래로 이동해 탐색 정보의 우선순위 개선

### 0.5.2

- 상위 카테고리의 직접 글 수와 하위 카테고리 글 수를 합산
- 티스토리 편집기가 생성한 `&nbsp;` 빈 문단 간격 정규화

## 5. 저장소 구조

```text
signal-tistory-skin/
├── AGENTS.md              # 에이전트용 시작 지침
├── HANDOFF.md             # 이 인수인계 문서
├── README.md              # 사용자용 설명과 설치법
├── skin.html              # 티스토리 스킨 템플릿
├── style.css              # 전체 스타일과 반응형 규칙
├── index.xml              # 버전, 기본값, 스킨 옵션
├── images/script.js       # 검색, 테마, 목차, 코드, 라이트박스
├── scripts/package.sh     # macOS/Linux/Git Bash ZIP 생성
├── assets/preview-source.svg
├── docs/plan.md
└── preview*.jpg/gif
```

`dist/`와 `*.zip`은 `.gitignore` 대상이다. 배포 ZIP은 Git에 커밋하지 않는다.

## 6. 다른 컴퓨터에서 시작하기

필수 준비물은 Git과 UTF-8 편집기다. 별도 npm 패키지 설치나 빌드 과정은 없다.

### macOS 또는 Linux

```bash
git clone https://github.com/bromine1997/signal-tistory-skin.git
cd signal-tistory-skin
git status --short --branch
git log -1 --oneline
```

ZIP 생성:

```bash
./scripts/package.sh
```

### Windows PowerShell

```powershell
git clone https://github.com/bromine1997/signal-tistory-skin.git
Set-Location signal-tistory-skin
git status --short --branch
git log -1 --oneline
```

Git Bash가 있다면 `./scripts/package.sh`를 그대로 실행한다. PowerShell만 사용할 때는 다음과 같이 ZIP을 만든다.

```powershell
$version = ([xml](Get-Content -Raw .\index.xml)).skin.information.version
$archive = ".\dist\Signal-v$version.zip"
New-Item -ItemType Directory -Force .\dist | Out-Null
if (Test-Path $archive) { Remove-Item $archive }
Compress-Archive -Path .\skin.html,.\style.css,.\index.xml,.\LICENSE,.\README.md,.\preview.gif,.\preview256.jpg,.\preview560.jpg,.\preview1600.jpg,.\images -DestinationPath $archive
Write-Output $archive
```

`.gitattributes`가 주요 텍스트 파일과 `package.sh`를 LF 줄바꿈으로 유지하므로 Windows와 macOS 사이의 불필요한 줄바꿈 변경을 방지한다.

## 7. 권장 작업 절차

1. `git pull --ff-only origin main`으로 최신 코드 받기
2. `index.xml`에서 다음 버전 변경
3. 관련 HTML, CSS, JavaScript 수정
4. `README.md` 변경 기록과 필요하면 이 문서 갱신
5. 문법과 diff 검사
6. 데스크톱·노트북·모바일 렌더링 확인
7. 커밋과 푸시
8. ZIP 생성 및 압축 내부 검사
9. 티스토리 관리자 화면에서 수동 적용

macOS/Linux 검사 명령:

```bash
git diff --check
node --check images/script.js
xmllint --noout index.xml
```

Windows에서 `xmllint`가 없다면:

```powershell
[xml](Get-Content -Raw .\index.xml) | Out-Null
node --check .\images\script.js
git diff --check
```

## 8. 티스토리 적용 방법

**GitHub에 푸시해도 티스토리에는 자동 반영되지 않는다.**

1. 패키징 스크립트나 PowerShell로 ZIP 생성
2. ZIP 압축 해제
3. 티스토리 관리 → 꾸미기 → 스킨 변경 → 스킨 등록
4. 압축을 푼 파일 등록
5. 스킨 보관함에서 선택 후 적용
6. 홈, 카테고리, 검색, 글, 댓글, 모바일 화면 확인

ZIP 루트의 필수 파일:

```text
skin.html
style.css
index.xml
images/script.js
```

`index.xml`을 다시 등록하면 기존 스킨 옵션이 초기화되거나 새 기본값이 적용될 수 있다. 적용 후 프로필 추가 소개 문구와 방문자 표시 옵션을 확인한다.

## 9. 개인정보 및 공개 배포 원칙

- 개인 GitHub, 포트폴리오, 이메일, 실명, 기술 스택을 스킨에 하드코딩하지 않는다.
- 프로필은 티스토리 치환자를 사용한다.
- 새 스킨 옵션의 기본값에 개인 정보를 넣지 않는다.
- 현재 저장소의 커밋 이메일은 `bromine1997@users.noreply.github.com`이다.

중요한 미해결 사항: 현재 소스에는 하드코딩된 개인 링크가 없지만 초기 Git 커밋 이력에는 과거 GitHub·포트폴리오 URL과 개인 이메일 커밋 정보가 남아 있다. 완전한 익명화에는 `git filter-repo` 등을 이용한 이력 재작성과 강제 푸시가 필요하다. 파괴적인 작업이므로 명시적인 승인 없이 실행하지 않는다.

## 10. 티스토리 호환 주의사항

- `[##_..._##]`는 티스토리 치환자이므로 일반 문자열로 바꾸지 않는다.
- `<s_...>`는 티스토리 조건·반복 요소다.
- `<s_search>`는 렌더링 시 래퍼 없이 입력창과 버튼만 남을 수 있다.
- 실제 본문에는 `.tt_article_useless_p_margin.contents_style`과 중첩 `div`가 들어갈 수 있다.
- 기본 `.another_category`는 커스텀 관련 글과 중복되어 숨긴 상태다.
- 댓글 그룹은 `<s_rp>` 안에 있어야 한다.
- 지연 로딩 이미지의 초기 너비가 `0`일 수 있으므로 그 값만으로 라이트박스 대상을 제외하지 않는다.
- 에디터가 코드 블록에 인라인 글자색을 넣을 수 있으므로 코드 색상 우선순위를 유지한다.

## 11. 후속 확인 항목

확정된 새 기능은 없다. 다음 기능을 시작하기 전에 사용자에게 방향을 확인한다.

- `0.5.2` 실제 티스토리 적용 후 홈·카테고리·검색·글·댓글 검수
- 미리보기 이미지가 `0.5.2` UI와 일치하는지 확인 및 갱신
- 실제 글에서 대형 화면 문단 폭과 이미지·코드 폭 확인
- Windows PowerShell 패키징 절차 실사용 검증
- 필요할 경우 초기 Git 이력의 개인정보 정리

## 12. 새 에이전트에게 전달할 프롬프트

```text
이 저장소의 AGENTS.md, HANDOFF.md, README.md를 먼저 전부 읽어줘.
현재 Signal 티스토리 스킨 버전과 git 상태를 확인하고,
공개 배포 원칙과 티스토리 치환자를 유지하면서 작업을 이어가자.
수정 전에는 관련 파일을 확인하고 수정 후에는 JS/XML/diff 검사와 반응형 검증을 해줘.
```
